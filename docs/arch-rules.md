# Архитектурные правила проекта

1. Ядро логики оформляйте как переиспользуемые модули в `app/`, а ноутбуки используются для ручной отладки.
2. Конфигурацию (ключи AFIP, пути, параметры запуска) храните в `.env`/`config` и не зашивайте в код.
3. Комментарии пишутся на русском языке.